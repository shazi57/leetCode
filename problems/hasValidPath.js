// const updateLocation = (road, position) => {
//   const [x, y] = position;
//   if (x === 0 && y === 0 && road) {

//   }
// };

const hasValidPath = (grid) => {
  // track visited cell and each cell's road
  // const visited = {};
  // // track current position
  // let curPos = [0, 0];
  // // track current road
  // let curRoad = grid[0][0];
  // // while current position is not grid[grid.length-1][grid.length[0]-1];
  // while (curPos[0] !== grid[grid.length - 1] && curPos[1] !== grid[0].length - 1) {
  //   // do the following
  //   // check if it's valid road, if not return false
  //   // check if it's valid position , if not return false
  // }
};

module.exports = hasValidPath;
